{
  "name": "@petforce/tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@petforce/core": "workspace:*",
    "vitest": "^2.0.0",
    "playwright": "^1.45.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0"
  }
}
